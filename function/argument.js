// 1. Define a function that takes two arguments and returns the sum of those arguments
function sum(a, b) {
    console.log(a + b);
}
sum(5, 10);
// 2. Define a function that takes two arguments and returns the difference of those arguments
function difference(a, b) {
    console.log(a - b);
}
difference(5, 10);
// 3. Define a function that takes 4 arguments and returns the product of those arguments

function data (name , age , gender ,city){
    console.log (`name: ${name}   age: ${age}   gender ${gender}    city: ${city}`);

}
data ("Abhishek", 22 , " male" , "delhi");
 // average of  numbers

    function average(a,b,c) {
        console.log(` This average : ${(a+b+c)/3}`);

    }
    average(4,5,10);
// 