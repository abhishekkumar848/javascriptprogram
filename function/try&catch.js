// try&catch is a reserved word in JavaScript, so it should be escaped.
// This is a test to make sure that the path is properly escaped.
 try{
    console.log(e);
 }
    catch{
        console.log("Error");
    } // Error
  